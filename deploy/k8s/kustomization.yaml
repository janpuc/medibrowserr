apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # - ingress.yaml   # enable after adjusting the host
images:
  - name: ghcr.io/janpuc/medibrowserr
    newTag: latest # pin to a version tag (e.g. "0.1.0") for reproducible deploys
